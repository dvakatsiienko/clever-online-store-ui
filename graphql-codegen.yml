schema: ${NEXT_PUBLIC_DEV_GQL_URL}
documents: 'src/graphql/**/*.gql'

config:
    # Test out the behavior and revisit later on
    enumsAsConst: true

generates:
    src/graphql/graphql.tsx:
        config:
            # documentMode: graphQLTag
            exportFragmentSpreadSubTypes: true
        plugins:
            - typescript
            - typescript-operations
            - typescript-react-apollo
            - typescript-apollo-client-helpers
    # src/graphql/ssr-typings.tsx:
    #     config:
    #         reactApolloVersion: 3
    #         gqlImport: '@apollo/client#gql'
    #         importDocumentNodeExternallyFrom: ./graphql
    #         # !! NEEDS TO BE INJECTED IN ORDER TO CREATE HOCS AUTOMATICALLY !!
    #         apolloClientInstanceImport: '../withApollo'
    #     preset: import-types
    #     presetConfig:
    #         typePaths: ./graphql
    #     plugins:
    #         - graphql-codegen-apollo-next-ssr
